{
    IBClasses = (
        {
            ACTIONS = {donate = id; selectPrayer = id; }; 
            CLASS = AppController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                appMenu = NSMenu; 
                asrItem = NSMenuItem; 
                dhuhurItem = NSMenuItem; 
                fajrItem = NSMenuItem; 
                ishaItem = NSMenuItem; 
                maghribItem = NSMenuItem; 
                shuruqItem = NSMenuItem; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}